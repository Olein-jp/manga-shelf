{
  "name": "manga-shelf",
  "version": "0.1.7",
  "private": true,
  "description": "Development environment for the Manga Shelf WordPress plugin.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:status": "wp-env status",
    "env:logs": "wp-env logs",
    "env:cli": "wp-env run cli wp"
  },
  "devDependencies": {
    "@wordpress/env": "11.13.0"
  },
  "overrides": {
    "adm-zip": "0.6.0"
  },
  "engines": {
    "node": ">=18.12.0",
    "npm": ">=8.19.2"
  }
}
